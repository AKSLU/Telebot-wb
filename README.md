# Telebot-wb
# Wildberries Analyzer Bot

Этот Telegram-бот анализирует карточку товара Wildberries: выводит название, рейтинг, историю цен, а также отвечает на вопросы, если они доступны через API.

## 📌 Функции бота

### `/start`
Показывает ваш Telegram ID, если вы в белом списке, иначе — сообщение об отказе доступа.

### `/info`
Получает название товара с Wildberries по `ARTICLE_ID`.

### `/price`
Анализирует историю цен:
- Средняя цена  
- Текущая цена  
- Результат («Выгодно», «Невыгодно», «Нейтрально»)

### `/rating`
Анализ рейтинга:
- Количество отзывов  
- Средний рейтинг  
- Оценка качества рейтинга

### `/question`
Получает список вопросов и ответов по товару с Wildberries.

## 📁 Структура проекта

```

project/
│
├── main.py
├── requirements.txt
├── README.md
│
├── confing/
│   └── confing.py
│
└── test/
└── test2.py

```

## 🔧 Настройка перед запуском

1. Установите зависимости:

```

pip install -r requirements.txt

````

2. В файле `confing/confing.py` укажите свои данные:

```python
WHITELIST = [ВАШ_ID]
CURRENCY_RATE = 6.44
ARTICLE_ID = "5007120"
````

3. В `main.py` вставьте ваш токен:

```python
token = "ВАШ_ТОКЕН_ОТ_@BotFather"
```

## ▶️ Запуск бота

```
python main.py
```

Бот начнёт работать через polling, после чего можно писать ему команды в Telegram.

## 🧪 Тесты

Тесты находятся в `test/test2.py`.

Запуск:

```
python test/test2.py
```

Проверяются:

* Работа whitelist
* Доступность карточки товара Wildberries
* Корректность функции логина

---

## 📦 Используемые библиотеки

* `requests` — запросы к API Wildberries
* `pyTelegramBotAPI` (telebot) — Telegram-бот

```
pip install requests telebot
```

---

# requirements.txt

```txt
requests
pyTelegramBotAPI
```
(Библиотека telebot устанавливается как pyTelegramBotAPI)



